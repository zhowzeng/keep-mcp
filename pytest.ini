[pytest]
addopts = -ra
asyncio_mode = auto
testpaths =
    tests/contract
    tests/integration
    tests/unit
    tests/perf
markers =
    contract: Contract-level MCP tool expectations
    integration: Multi-component behaviour checks
    unit: Unit tests for services and repositories
    perf: Performance and latency checks
